name: inheritable
description: Efficient by default zero-dependency declarative state/dependency management for flutter
homepage: https://github.com/devkabiir/inheritable
repository: https://github.com/devkabiir/inheritable
issue_tracker: https://github.com/devkabiir/inheritable/issues
version: 1.0.0-beta.1

environment:
  sdk: '>=2.12.0 <3.0.0'
  flutter: '>=2.0.0'

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.1.8

dev_dependencies:
  flutter_test:
    sdk: flutter
